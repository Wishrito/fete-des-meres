{% extends "models/base.j2" %}
{% block title %}Accueil - Fête des Mères{% endblock %}

{% block content %}
<section class="hero-section">
    <div class="container">
        <h1 class="display-4 fw-bold mb-4">
            Bonne Fête des Mères
        </h1>
        <h2 class="h3 mb-4">Pour une mère qui assure</h2>
        <p class="lead mb-5">
            Ce site, c'est ma façon de te dire merci (à l'aide de mes connaissances) pour tout ce que tu fais,
            et de reconnaître la femme forte que tu es.
        </p>
        <a href="{{ url_for('messages') }}" class="btn btn-primary btn-lg">
            <i class="fas fa-message"></i> Voir mes messages
        </a>
    </div>
</section>

<section class="py-5">
    <div class="container">
        <h2 class="section-title h1">Tes plus belles qualités</h2>
        <div class="row">
            {% for qualite in maman.qualites %}
            <div class="col-md-4 mb-4">
                <div class="card h-100 text-center p-4">
                    <div class="card-body">
                        <i class="fas fa-star text-warning mb-3" style="font-size: 2em;"></i>
                        <h5 class="card-title text-primary">{{ qualite.adjectif }}</h5>
                        <p class="card-text text-muted">
                            {% if qualite.description %}
                            {{ qualite.description }}
                            {% else %}
                            Une qualité qui te rend unique et précieuse à nos yeux.
                            {% endif %}
                        </p>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</section>

<section class="py-5 bg-light">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-lg-6">
                <h2 class="section-title">Ce que j'admire chez toi</h2>
                <div class="list-group list-group-flush">
                    <div class="list-group-item border-0 bg-transparent">
                        <i class="fas fa-check-circle accent-color me-3"></i>
                        <strong>Ta force</strong> dans les moments difficiles
                    </div>
                    <div class="list-group-item border-0 bg-transparent">
                        <i class="fas fa-lightbulb accent-color me-3"></i>
                        <strong>Ton intelligence</strong> et tes conseils pertinents
                    </div>
                    <div class="list-group-item border-0 bg-transparent">
                        <i class="fas fa-smile accent-color me-3"></i>
                        <strong>Ton humour</strong> qui détend toutes les situations
                    </div>
                    <div class="list-group-item border-0 bg-transparent">
                        <i class="fas fa-handshake accent-color me-3"></i>
                        <strong>Ton respect</strong> pour mes choix et mon indépendance
                    </div>
                </div>
            </div>
            <div class="col-lg-6 text-center">
                <div class="card border-0 shadow-lg">
                    <div class="card-body p-5">
                        <i class="fas fa-quote-left accent-color mb-3" style="font-size: 3em;"></i>
                        <blockquote class="blockquote">
                            <p class="mb-4 fst-italic">
                                "{{ citation}}"
                            </p>
                        </blockquote>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
{% endblock %}